import { CalculateCaloriesPipe } from './calculate-calories.pipe';

describe('CalculateCaloriesPipe', () => {
  it('create an instance', () => {
    const pipe = new CalculateCaloriesPipe();
    expect(pipe).toBeTruthy();
  });
});
