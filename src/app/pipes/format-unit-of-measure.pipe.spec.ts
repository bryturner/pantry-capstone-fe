import { FormatUnitOfMeasurePipe } from './format-unit-of-measure.pipe';

describe('FormatUnitOfMeasurePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatUnitOfMeasurePipe();
    expect(pipe).toBeTruthy();
  });
});
