import { FilterIngredientNamePipe } from './filter-ingredient-name.pipe';

describe('FilterIngredientNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIngredientNamePipe();
    expect(pipe).toBeTruthy();
  });
});
