import { FilterRecipeByUserPipe } from './filter-recipe-by-user.pipe';

describe('FilterRecipeByUserPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterRecipeByUserPipe();
    expect(pipe).toBeTruthy();
  });
});
